// this file is to include various optimization params used at compile time
// all params are decided by makefile

// how many concurrency cpu cores (hyperthreading counts) we can use
const int NUM_CPU_CORE=8 ;
